Clone this repository to your local machine:
https://github.com/ysvvteja/order-management-api1.git

Open the project in IntelliJ

Reload Maven dependencies to ensure all required libraries (like Lombok) are installed.
In IntelliJ: Right-click pom.xml → Reload Project

Run the Spring Boot application:
Locate the DemoApplication.java file.
Click the Run button to start the app.

Once the application starts, open Postman (or any API client).
http://localhost:8080/api/orders
